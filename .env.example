# FitLoot — variáveis de ambiente
# Copie para .env ou use .dev.vars para wrangler (dev local)

# API Anthropic (para recursos de IA)
# Definir em producao: wrangler secret put ANTHROPIC_API_KEY
ANTHROPIC_API_KEY=

# Frontend (opcional — em dev com proxy use '' para mesmo origin)
# VITE_API_URL=https://fitloot-worker.suportefitloot.workers.dev
